# AP-Final-Project-Phase3
The last phase of my AP course final project;
to execute run "./utrip.out hotels.csv" and then type "localhost:3000" in a web browser

This phase tries to simulate a simple HTML-CSS hotel booking site (such as booking.com) which covers some functions from the last two phases.
